<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Gestion des Réservations</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/special.css">
</head>
<body>
    <header><h1>Gestion des Réservations</h1></header>

    <main class="catway-container">
        <section class="add-section">
            <h3>Nouvelle Réservation</h3>
            <form action="/reservations/add" method="POST">
                <input type="number" name="catwayNumber" placeholder="N° Catway" required>
                <input type="text" name="clientName" placeholder="Nom Client" required>
                <input type="text" name="boatName" placeholder="Nom Bateau" required>
                
                <label for="startDate">Date de début :</label>
                <input type="date" id="startDate" name="startDate" required>
                
                <label for="endDate">Date de fin :</label>
                <input type="date" id="endDate" name="endDate" required>
                
                <button type="submit">Réserver</button>
            </form>
        </section>

        <table>
            <thead>
                <tr>
                    <th>N° Catway</th>
                    <th>Client</th>
                    <th>Bateau</th>
                    <th>Dates</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% reservations.forEach(function(res) { %>
                    <tr>
                        <td><%= res.catwayNumber %></td>
                        <td><%= res.clientName %></td>
                        <td><%= res.boatName %></td>
                        <td><%= res.startDate.toLocaleDateString() %> - <%= res.endDate.toLocaleDateString() %></td>
                        <td>
                            <a href="/reservations/edit/<%= res._id %>">Modifier</a>
                            <a href="/reservations/delete/<%= res._id %>" onclick="return confirm('Confirmer ?')">Supprimer</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </main>
    <footer><a href="/dashboard">← Retour au Dashboard</a></footer>
</body>
</html>